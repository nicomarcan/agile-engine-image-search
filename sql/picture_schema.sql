CREATE TABLE pictures (
    id text NOT NULL,
    cropped_picture text NOT NULL,
    author text NOT NULL,
    camera text NOT NULL,
    tags text NOT NULL,
    full_picture text NOT NULL,
    PRIMARY KEY (ID)
);